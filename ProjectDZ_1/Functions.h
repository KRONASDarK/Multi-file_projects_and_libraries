#pragma once

#include <cmath>
#include <iostream>
#include <typeinfo>

// Объявления функций
extern void Enter(float& x, float& y);
extern float Sum(float x, float y);
extern float Sub(float x, float y);
extern float Div(float x, float y);
extern float Multi(float x, float y);
extern float Degree(float x, float y);
extern void PrintInfo(float x, float y);